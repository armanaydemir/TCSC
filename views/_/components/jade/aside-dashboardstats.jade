html
  body
    .container.col-lg-4.col-md-4.col-sm-4(style='margin-top: 5em;')
      .panel.panel-default
        .panel-heading
          h3.panel-title
            | TEAM NAME HERE
            span(style='float:right;padding-right:0.5em;')
              img.img-rounded(src='images/useridenticon.png', width='20px', height='20px')
        .panel-body
          p(style='line-height:0.1em')
            | Points
            span(style='float:right; color:dimgrey; padding-right:1em;') 0
          hr
          p(style='line-height:0.1em')
            | Questions Answered
            span(style='float:right; color:dimgrey; padding-right:1em;') 0
          hr
          p(style='line-height:0.1em')
            | Other
            span(style='float:right; color:dimgrey; padding-right:1em;') 0
      .panel.panel-default
        .panel-heading
          h3.panel-title  Team Name Chat 
        .panel-body
          ul#messages
            form(action='')
              input#m(autocomplete='off')
              button Send
script.
  var socket = io();
  $(function(){
    $('button').click(function(){
      socket.emit('register_team', $('#m').val());
      $('#messages').append($('<li>').text(msg));
      $('#m').val('');
      return false;
    });
  });
  
  socket.on('new_message' + #{config.user.team}, function(data){
    $('#messages').append($('<li>').text(msg));
  });